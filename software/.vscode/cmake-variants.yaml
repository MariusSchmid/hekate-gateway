buildType:
  default: debug
  choices:
    debug:
      short: debug
      long: debug with example
      buildType: Debug
      settings:
        PICO_BOARD: pico_w
        INTERNET_WIFI: OFF
        INTERNET_SIM: ON
