buildType:
  default: SIM
  choices:
    SIM:
      short: SIM
      long: debug with SIM enabled
      buildType: Debug
      settings:
        PICO_BOARD: pico_w
        INTERNET_WIFI: ON
        INTERNET_SIM: OFF
    WIFI:
      short: WIFI
      long: debug with WIFI enabled
      buildType: Debug
      settings:
        PICO_BOARD: pico_w
        INTERNET_WIFI: ON
        INTERNET_SIM: OFF
